import { defineConfig } from 'vite';

export default defineConfig({
    base: '/BlackJack-Vite-js/',  // es el directorio o nombre que aparece en GITHUB para acceder a nuestra app (sin contar el dominio de GitHub que tengamos):
    // https://vangeyper.github.io/BlackJack-Vite-js/
});